# Docker Backup & Restore Tool

![Bash](https://img.shields.io/badge/Bash-4.0%2B-blue)
![Docker](https://img.shields.io/badge/Docker-required-green)
![License](https://img.shields.io/badge/License-MIT-lightgrey)

Скрипт для создания резервных копий Docker окружения и восстановления в изолированных сетях.

## 📋 Оглавление

- [Возможности](#возможности)
- [Предварительные требования](#предварительные-требования)
- [Установка](#установка)
- [Использование](#использование)
- [Примеры](#примеры)
- [Структура архива](#структура-архива)
- [Восстановление в закрытом контуре](#восстановление-в-закрытом-контуре)
- [Устранение неисправностей](#устранение-неисправностей)

## 🚀 Возможности

- **Автоматическое обнаружение** docker-compose.yml
- **Сохранение всех Docker образов** из композ-файла
- **Копирование зависимых файлов**: Dockerfile, init-скрипты, контексты сборки
- **Интерактивное именование** архивов с timestamp
- **Автообнаружение существующих архивов** для восстановления
- **Автоматическое создание скрипта восстановления**
- **Цветной вывод** с подробным логированием
- **Поддержка кастомных путей** к docker-compose.yml

## ⚙️ Предварительные требования

- **Bash** 4.0 или выше
- **Docker** установленный и запущенный
- **Docker Compose** (docker-compose или docker compose)
- **tar** для работы с архивами

## 📥 Установка

1. **Скачайте скрипт (github):**
```bash
wget -O docker_backup.sh https://raw.githubusercontent.com/kolelan/transferring-project/refs/heads/main/docker_backup.sh

```
1.1 **Или скачайте скрипт (gitverse)**
```bash
wget -O docker_backup.sh https://gitverse.ru/api/repos/Kolelan/transferring-project/raw/branch/main/docker_backup.sh

```
2. **Сделайте скрипт исполняемым:**
```bash
chmod +x docker_backup.sh
```

3. **Проверьте зависимости:**
```bash
./docker_backup.sh
```

## 🎯 Использование

### Базовое использование

```bash
./docker_backup.sh
```

Скрипт предложит:
1. **Автоматически найти** существующие архивы для восстановления
2. **Выбрать режим работы**: сохранение или восстановление
3. **Указать имя архива** (при сохранении)
4. **Подтвердить действия**

### Режим сохранения

```bash
# Запуск в режиме сохранения
./docker_backup.sh

# Будет выполнено:
# ✅ Найден docker-compose.yml
# ✅ Извлечены все образы
# ✅ Сохранены образы в .tar файлы
# ✅ Скопированы init-скрипты и Dockerfile
# ✅ Создан архив с указанным именем
```

### Режим восстановления

```bash
# Запуск в режиме восстановления
./docker_backup.sh

# Или прямое указание архива
./docker_backup.sh
# Затем выбрать опцию 2 и указать путь к архиву
```

## 📝 Примеры

### Пример 1: Сохранение проекта

```bash
$ ./docker_backup.sh
[INFO] Скрипт сохранения/восстановления Docker окружения

[INFO] Найдены архивы для восстановления:
  1. resore_docker_myproject_20241210_143022.tar.gz

Хотите распаковать один из архивов? (y/N): n

[INFO] Режим работы:
  1. Сохранить текущее окружение в архив
  2. Восстановить из конкретного архива
  3. Выйти

Выберите действие (1-3): 1

[INFO] Найден docker-compose.yml в текущей директории
[INFO] Получение списка образов из docker-compose.yml...
[INFO] Найдено образов: 4
[INFO] Обработка: postgres:9.6-alpine
[INFO] Обработка: redis:alpine
[INFO] Обработка: dpage/pgadmin4
[INFO] Обработка: custom-app:latest

Введите название архива (без расширения): myproject

[INFO] Создание архива ./resore_docker_myproject_20241210_143022.tar.gz...
[SUCCESS] Архив создан: ./resore_docker_myproject_20241210_143022.tar.gz
Размер архива: 1.2G
```

### Пример 2: Восстановление в закрытом контуре

```bash
# В закрытом контуре после переноса архива:
$ ./docker_backup.sh
[INFO] Найдены архивы для восстановления:
  1. resore_docker_myproject_20241210_143022.tar.gz

Хотите распаковать один из архивов? (y/N): y
Введите номер архива (1-1): 1

[INFO] Восстановление из архива: resore_docker_myproject_20241210_143022.tar.gz
Директория restore_docker уже существует. Перезаписать? (y/N): y
[INFO] Распаковка архива...
[SUCCESS] Архив распакован в директорию: restore_docker

[INFO] Для запуска проекта выполните:
  cd restore_docker
  ./restore.sh
```

## 📁 Структура архива

```
resore_docker_имя_проекта_ГГГГММДД_ЧЧММСС.tar.gz
├── docker-compose.yml          # Оригинальный композ-файл
├── images/                     # Директория с образами
│   ├── postgres-9.6-alpine.tar
│   ├── redis-alpine.tar
│   ├── dpage-pgadmin4.tar
│   └── custom-app-latest.tar
├── init-db/                    # Скрипты инициализации БД (если есть)
│   ├── 01-init.sql
│   └── 02-data.sql
├── build_context/              # Контекст для кастомных образов (если есть)
│   ├── Dockerfile
│   ├── app/
│   └── requirements.txt
├── restore.sh                  # Скрипт автоматического восстановления
└── README.md                   # Инструкция по восстановлению
```

## 🔧 Восстановление в закрытом контуре

### Автоматическое восстановление

```bash
cd restore_docker
./restore.sh
```

### Ручное восстановление

```bash
# 1. Распаковать архив
tar -xzf resore_docker_myproject_20241210_143022.tar.gz
cd restore_docker

# 2. Загрузить образы
for file in images/*.tar; do
    echo "Загрузка: $file"
    docker load -i "$file"
done

# 3. Запустить сервисы
docker-compose up -d

# 4. Проверить статус
docker-compose ps
docker-compose logs
```

## 🛠️ Устранение неисправностей

### Проблема: "Команда docker не найдена"
**Решение:** Установите Docker и убедитесь, что он добавлен в PATH

### Проблема: "Не удалось получить список образов"
**Решение:** Проверьте синтаксис docker-compose.yml и доступность образов

### Проблема: "Недостаточно места на диске"
**Решение:** Освободите место или укажите другую директорию для архива

### Проблема: "Ошибка прав доступа"
**Решение:** Запустите скрипт с sudo или добавьте пользователя в группу docker

```bash
sudo usermod -aG docker $USER
newgrp docker
```

### Проблема: Образы не загружаются в закрытом контуре
**Решение:** Проверьте совместимость версий Docker и архитектуру процессора

## 🔄 Дополнительные возможности

### Прямое указание docker-compose.yml

```bash
# Если docker-compose.yml в нестандартном месте
./docker_backup.sh
# При запросе укажите полный путь: /path/to/docker-compose.yml
```

### Пакетное использование

```bash
# Для автоматизации можно модифицировать скрипт:
echo "1" | ./docker_backup.sh                    # Автовыбор сохранения
echo -e "1\nmyproject" | ./docker_backup.sh      # Автовыбор с именем проекта
```

## 📄 Лицензия

MIT License - вы можете свободно использовать и модифицировать скрипт.

## 🤝 Вклад в разработку

Нашли ошибку или есть предложения? Создайте issue или pull request в репозитории проекта.

---

**Примечание:** Всегда тестируйте восстановление в тестовой среде перед использованием.